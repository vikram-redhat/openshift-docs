version: 2.1

jobs:
  build-and-test:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - checkout
      - run:
          command: | 
            gem install asciidoctor
            gem install asciidoctor-diagram
            pip install --upgrade pip            
            pip install pyyaml
            pip install aura.tar.gz
            python build.py --distro openshift-enterprise --product "OpenShift Container Platform" --version 4.7 --no-upstream-fetch && python makeBuild.py
            bash autopreview.sh
            bash autocomment.sh
          name: install and prereq
workflows:
  main:
    jobs:
      - build-and-test
